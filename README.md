# 古籍活化系统 (Gujihuohua)

一个功能完整的古籍文献处理和分析平台，集成了文本处理、自然语言处理(NLP)、知识图谱等功能。该系统支持古籍文献的上传、处理、存储和多维度分析。

## 📋 项目概述

古籍活化系统是一个基于 **Spring Boot + Vue 3** 的前后端分离应用，专门用于古籍文献的数字化处理和深度分析。系统提供了完整的文本处理管道，包括文献上传、句子分割、命名实体识别(NER)、关系抽取、事件分析等功能。

### 核心特性

- 🔧 **文本处理引擎** - 支持多种文件格式上传、字符编码检测、句子自动分割
- 🏷️ **命名实体识别(Ner)** - 提取古籍中的人物、地点、机构等实体
- 🔗 **人物关系抽取(Rel)** - 挖掘文献中实体之间的关系网络
- 📊 **事件分析** - 提取和组织历史事件信息
- 👤 **人物画像生成(Pof)** - 构建历史人物的多维度档案
- 📚 **文献库管理** - 完整的CRUD操作和元数据管理
- 💾 **分层存储** - 按日期自动归档，支持JSON和TXT双格式保存

## 🏗️ 技术栈

### 后端 (Backend)
- **框架**: Spring Boot 2.7.1
- **Java版本**: Java 8
- **数据库**: MySQL 8.2
- **ORM**: MyBatis-Plus 3.5.5
- **JSON处理**: FastJSON 1.2.83
- **NLP工具**: 
  - OpenCC4J 1.8.1 (繁简转换)
  - JUniversalChardet 2.4.0 (字符编码检测)
- **其他**: Lombok, Spring Data JPA

### 前端 (Frontend)
- **框架**: Vue 3
- **UI组件库**: Element Plus 2.11.8
- **图表库**: ECharts 6.0.0
- **HTTP客户端**: Axios 1.13.2
- **构建工具**: Vue CLI 5.0
- **样式**: CSS3 + 传统书法字体(楷体)

## 📁 项目结构

```
gujihuohua-master/
├── src/
│   ├── main/
│   │   ├── java/com/example/gujihuohua/
│   │   │   ├── GujihuohuaApplication.java     # Spring Boot主程序
│   │   │   ├── controller/                     # 控制层
│   │   │   │   ├── DictController.java        # 字典与词汇查询API
│   │   │   │   ├── LibraryController.java     # 文献库API
│   │   │   │   ├── TextProcessController.java # 文本处理API
│   │   │   │   ├── NerController.java         # 命名实体识别（NER）分析API
│   │   │   │   ├── RelController.java         # 关系（REL）分析API
│   │   │   │   └── PofController.java         # 人物画像（POF）分析API
│   │   │   ├── service/                        # 业务层
│   │   │   │   ├── DictService.java           # 字典数据导入与管理
│   │   │   │   ├── LibraryService.java        # 文献库业务逻辑
│   │   │   │   ├── TextProcessService.java    # 文本处理业务逻辑
│   │   │   │   ├── NerService.java            # 命名实体识别业务逻辑
│   │   │   │   ├── RelService.java            # 关系分析业务逻辑
│   │   │   │   ├── PofService.java            # 人物画像业务逻辑接口
│   │   │   │   ├── AiModelService.java        # AI模型调用服务
│   │   │   │   └── pof_impl/
│   │   │   │       └── PofServiceImpl.java    # 人物画像实现（调用5001/5002端口）
│   │   │   ├── entity/                         # 数据实体
│   │   │   │   ├── AncientText.java           # 古籍文献实体
│   │   │   │   ├── XinhuaWord.java            # 新华字典实体
│   │   │   │   ├── XinhuaCi.java              # 新华词语实体
│   │   │   │   ├── XinhuaIdiom.java           # 新华成语实体
│   │   │   │   └── XinhuaXiehouyu.java        # 新华歇后语实体
│   │   │   ├── mapper/                         # 数据访问层
│   │   │   │   ├── AncientTextMapper.java     # 古籍映射器
│   │   │   │   ├── XinhuaWordMapper.java      # 字典映射器
│   │   │   │   ├── XinhuaCiMapper.java        # 词语映射器
│   │   │   │   ├── XinhuaIdiomMapper.java     # 成语映射器
│   │   │   │   └── XinhuaXiehouyuMapper.java  # 歇后语映射器
│   │   │   ├── config/                         # 配置类
│   │   │   └── data/                           # 数据类
│   │   │       ├── TextProcessRequest.java    # 处理请求DTO
│   │   │       └── SentenceResult.java        # 处理结果DTO
│   │   └── resources/
│   │       ├── application.properties          # 配置文件
│   │       ├── rawData/                        # 原始字典数据
│   │       │   ├── word.json                  # 汉字数据
│   │       │   ├── ci.json                    # 词语数据
│   │       │   ├── idiom.json                 # 成语数据
│   │       │   └── xiehouyu.json              # 歇后语数据
│   │       └── sql_file/                       # 数据库脚本
│   │           ├── gujihuohua.sql             # 主数据库初始化脚本
│   │           ├── ancient_text.sql           # 古籍文献表
│   │           ├── xinhua_word.sql            # 汉字字典表
│   │           ├── xinhua_ci.sql              # 词语字典表
│   │           ├── xinhua_idiom.sql           # 成语字典表
│   │           └── xinhua_xiehouyu.sql        # 歇后语字典表
│   └── test/                                    # 测试代码
├── gujihuohua_vue/                              # 前端Vue项目
│   ├── src/
│   │   ├── App.vue                             # 根组件
│   │   ├── main.js                             # 入口文件
│   │   ├── components/
│   │   │   ├── WelcomePage.vue                # 欢迎页面
│   │   │   ├── SideMenu.vue                   # 左侧菜单
│   │   │   ├── TextProcess.vue                # 文本处理组件
│   │   │   ├── TextLibrary.vue                # 文献库管理
│   │   │   ├── FileUpload.vue                 # 文件上传组件
│   │   │   ├── NerAnalysis.vue                # NER分析组件
│   │   │   ├── RelAnalysis.vue                # 关系分析组件
│   │   │   ├── EventAnalysis.vue              # 事件分析组件
│   │   │   ├── PofAnalysis.vue                # 人物画像组件
│   │   │   ├── XinhuaDict.vue                 # 新华字典查询组件
│   │   │   └── DictTable.vue                  # 字典数据表格组件
│   │   └── assets/                             # 静态资源
│   ├── public/
│   │   └── index.html                          # HTML入口
│   ├── package.json                            # npm依赖
│   └── vue.config.js                           # Vue CLI配置
├── data-storage/                                # 数据存储目录
│   ├── 2025-11-21/                             # 按日期分组
│   └── 2025-11-24/
├── pom.xml                                      # Maven配置
├── mvnw & mvnw.cmd                             # Maven包装器
├── LICENSE                                      # 开源协议
└── README.md                                    # 项目说明(本文件)
```

## 🚀 快速开始

### 前置环境

- **Java**: JDK 8+
- **Maven**: 3.6+
- **Node.js**: 14+
- **MySQL**: 5.7+

### 后端启动

1. **环境配置**
   ```bash
   # 修改数据库连接配置
   # src/main/resources/application.properties
   spring.datasource.url=jdbc:mysql://localhost:3306/gujihuohua?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
   spring.datasource.username=root
   spring.datasource.password=123456
   ```

2. **数据库初始化**
   ```bash
   # 导入SQL脚本到MySQL
   mysql -u root -p gujihuohua < src/main/resources/gujihuohua.sql
   ```

3. **构建与运行**
   ```bash
   # 使用Maven构建
   mvnw.cmd clean package
   
   # 运行应用
   mvnw.cmd spring-boot:run
   
   # 或者直接运行JAR包
   java -jar target/gujihuohua-0.0.1-SNAPSHOT.jar
   ```

   后端启动后访问: `http://localhost:8080`

### 前端启动

1. **安装依赖**
   ```bash
   cd gujihuohua_vue
   npm install
   ```

2. **开发模式运行**
   ```bash
   npm run serve
   ```
   前端开发服务器: `http://localhost:8081`

3. **生产环境构建**
   ```bash
   npm run build
   # 输出文件在 dist/ 目录
   ```

## 📖 API文档

### 字典查询接口

#### 1. 初始化字典数据
**POST** `/api/dict/init`

用于首次导入新华字典、成语、歇后语等数据到数据库。

响应:
```json
"字典数据导入成功"
```

#### 2. 字典搜索
**GET** `/api/dict/search`

查询字、词语、成语、歇后语等。

请求参数:
- `type` (string): 查询类型，可选值：
  - `word` - 查汉字
  - `ci` - 查词语  
  - `idiom` - 查成语 (默认)
  - `xiehouyu` - 查歇后语
- `keyword` (string): 搜索关键词

响应示例:
```json
[
  {
    "id": 1,
    "word": "高",
    "explanation": "同\"高\"...",
    ...其他字典相关字段
  }
]
```

请求示例:
```bash
# 查汉字
GET /api/dict/search?type=word&keyword=高

# 查成语
GET /api/dict/search?type=idiom&keyword=心想事成

# 查歇后语  
GET /api/dict/search?type=xiehouyu&keyword=八仙
```

### 文本处理接口

#### 1. 处理文本
**POST** `/api/text/process`

请求参数 (multipart/form-data):
- `file`: 上传的文件 (可选)
- `config`: JSON格式的处理配置
  - `content`: 直接粘贴的文本内容 (当file为空时使用)
  - 其他处理参数...

响应示例:
```json
{
  "totalSentences": 10,
  "lines": [
    {
      "sentenceId": 1,
      "content": "高祖年十八...",
      "entities": []
    }
  ]
}
```

#### 2. 文字类型自动检测
**POST** `/api/text/detect-type`

请求:
```json
{
  "content": "古籍文本内容"
}
```

响应:
```json
"古籍类型"
```

### 文献库接口

#### 1. 保存文献
**POST** `/api/library/save`

请求体:
```json
{
  "title": "史记—高祖本纪",
  "category": "历史",
  "rawContent": "原始文本内容",
  "count": 25,
  "data": [...]  // 处理后的数据
}
```

响应: `保存成功`

#### 2. 获取文献列表
**GET** `/api/library/list`

响应:
```json
[
  {
    "id": 1,
    "title": "史记—高祖本纪",
    "category": "历史",
    "sentenceCount": 25,
    "createTime": "2025-11-24T15:18:56"
  }
]
```

#### 3. 获取文献详情
**GET** `/api/library/detail/{id}`

响应:
```json
{
  "info": {
    "id": 1,
    "title": "...",
    "category": "...",
    "rawPath": "2025-11-24/xxx.txt",
    "jsonPath": "2025-11-24/xxx.json"
  },
  "processedJson": "[...]"
}
```

#### 4. 删除文献
**DELETE** `/api/library/{id}`

响应: `已删除`

## ⚙️ 配置说明

### application.properties 核心配置

```properties
# 应用信息
spring.application.name=gujihuohua
server.port=8080

# 数据库配置
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/gujihuohua?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=123456

# 存储配置
app.storage.root-path=./data-storage  # 文献文件存储根目录
```

### 存储结构

系统采用按日期分组的存储策略：
```
data-storage/
├── 2025-11-21/
│   ├── 史记—高祖本纪_20251121_102455.txt
│   └── 史记—高祖本纪_20251121_102455.json
├── 2025-11-24/
│   ├── 高祖_20251124_120911.txt
│   ├── 高祖_20251124_120911.json
│   └── ...
```

**命名规则**: `{标题}_{时间戳}.{扩展名}`
- **标题**: 用户输入的自定义名称
- **时间戳**: yyyyMMdd_HHmmss格式

## 🔄 工作流程

### 文献处理完整流程

1. **上传文献**
   - 用户在TextProcess组件中上传文件或粘贴文本
   - 系统检测文件编码

2. **处理分析**
   - 调用`/api/text/process`进行文本处理
   - 自动分割句子
   - 提取文本特征

3. **保存归档**
   - 生成JSON和TXT两种格式的文件
   - 文件按日期存储到`data-storage/`
   - 元数据保存到MySQL数据库

4. **多维分析**
   - NER分析：提取人物、地点等实体
   - 关系分析：构建实体关系图
   - 事件分析：抽取历史事件
   - 人物画像：生成人物档案

5. **库管理查询**
   - 通过TextLibrary组件浏览已保存文献
   - 查看详情、删除等操作

## 🎨 主要功能模块

### 1. 文本处理 (TextProcess.vue)
- 文件上传（支持多格式）
- 直接粘贴输入
- 字符编码自动检测
- 句子自动分割
- 实时预览处理结果

### 2. 文献库管理 (TextLibrary.vue)
- 查看所有已保存的文献
- 按分类、时间等条件筛选
- 查看文献详情
- 删除过期文献
- 数据备份导出

### 3. NER分析 (NerAnalysis.vue)
- 从文献中提取人物、地点、机构等实体
- 实体类型标注
- 出现频率统计
- 可视化展示

### 4. 关系分析 (RelationAnalysis.vue)
- 构建知识图谱
- 实体关系抽取
- 关系类型分类
- 图谱可视化

### 5. 事件分析 (EventAnalysis.vue)
- 历史事件抽取
- 事件时间线构建
- 事件参与者分析
- 事件关联性分析

### 6. 人物画像 (PofAnalysis.vue)
- 历史人物特征提取
- 实体识别（人物/地点/官职/时间）
- 语义相似度分析
- 共现关系分析
- 调用外部NLP服务（端口5001/5002）

### 7. 新华字典 (XinhuaDict.vue)
- 汉字/词语/成语/歇后语查询
- 支持精确查询和模糊匹配
- 字典数据表格展示（DictTable.vue）

### 8. 字典管理 (DictController)
- 新华字典数据导入和管理
- 支持汉字、词语、成语、歇后语的多维查询
- 支持模糊匹配和精确查询
- 返回结果限制为50条以提高查询效率
- 为NLP分析和文献注解提供词汇支持

## 📊 数据库设计

### AncientText 表 - 古籍文献

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| title | VARCHAR(255) | 文献标题 |
| category | VARCHAR(100) | 分类(历史/文学/哲学等) |
| sentence_count | INT | 句子总数 |
| raw_path | VARCHAR(255) | 原始文本相对路径 |
| json_path | VARCHAR(255) | JSON处理结果相对路径 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

### XinhuaWord 表 - 汉字字典

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| word | VARCHAR(10) | 汉字 |
| pinyin | VARCHAR(50) | 拼音 |
| stroke_count | INT | 笔画数 |
| radical | VARCHAR(10) | 部首 |
| explanation | TEXT | 字义解释 |

### XinhuaCi 表 - 词语字典

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| ci | VARCHAR(100) | 词语 |
| explanation | TEXT | 词义解释 |
| example | VARCHAR(255) | 使用例句 |

### XinhuaIdiom 表 - 成语字典

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| word | VARCHAR(20) | 成语 |
| pinyin | VARCHAR(100) | 拼音 |
| meaning | TEXT | 成语含义 |
| origin | TEXT | 出处 |
| example | TEXT | 使用例句 |

### XinhuaXiehouyu 表 - 歇后语字典

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| riddle | VARCHAR(100) | 谜面（问句部分） |
| answer | VARCHAR(100) | 谜底（答案部分） |
| category | VARCHAR(50) | 分类 |

## 🛠️ 开发指南

### 添加新的分析组件

1. 创建Vue组件文件: `src/components/NewAnalysis.vue`
2. 在`App.vue`中导入并注册
3. 在`SideMenu.vue`中添加菜单项
4. 后端添加对应的Controller和Service

### 扩展文本处理功能

1. 在`TextProcessService`中添加新的处理方法
2. 创建对应的DTO类存储处理结果
3. 在`TextProcessController`中创建新的API端点
4. 前端调用新API

### 数据库迁移

1. 修改`gujihuohua.sql`脚本
2. 运行脚本更新数据库结构
3. 更新对应的Entity和Mapper

## 📝 常见问题

### Q: 如何修改数据存储路径?
A: 在`application.properties`中修改`app.storage.root-path`配置项

### Q: 系统支持哪些文件格式?
A: 支持TXT、JSON及其他文本格式，系统会自动检测编码

### Q: 如何进行大批量文献导入?
A: 可以编写脚本调用`/api/library/save`API进行批量保存

### Q: 前后端如何联调?
A: 确保后端运行在8080端口，前端开发服务器会代理API请求到后端

### Q: 字典数据如何初始化?
A: 首次启动后调用`POST /api/dict/init`接口导入新华字典、成语、歇后语等数据到数据库

### Q: 如何更新或扩展字典数据?
A: 修改`src/main/resources/rawData/`目录下的JSON文件（word.json、ci.json、idiom.json、xiehouyu.json），然后调用初始化接口重新导入

### Q: 字典查询支持哪些搜索方式?
A: 支持精确查询和模糊匹配（like），结果限制为50条以提高性能

## 📄 许可证

MIT License - 查看[LICENSE](LICENSE)文件获取详情

## 👥 贡献指南

欢迎提交Issue和Pull Request！请确保：
- 遵循现有的代码风格
- 添加单元测试
- 更新相关文档

## 📞 联系方式

如有问题或建议，欢迎通过以下方式联系：
- 提交GitHub Issue：https://github.com/1122pcd1122/gujihuohua.git
- 发送邮件讨论

---

**最后更新**: 2025年12月14日
